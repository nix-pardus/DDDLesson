<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ControlsCollection.MonthYearGroupedSelector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ControlsCollection"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <CollectionViewSource x:Name="MonthsCVS" IsSourceGrouped="True"/>
    </UserControl.Resources>

    <ListView ItemsSource="{x:Bind MonthsCVS.View, Mode=OneWay}"
              SelectionChanged="ListView_SelectionChanged"
              SelectionMode="Single"
              IsItemClickEnabled="True"
              ItemClick="ListView_ItemClick">
        <ListView.ItemsPanel>
            <ItemsPanelTemplate>
                <ItemsStackPanel AreStickyGroupHeadersEnabled="True"/>
            </ItemsPanelTemplate>
        </ListView.ItemsPanel>

        <ListView.GroupStyle>
            <GroupStyle>
                <GroupStyle.HeaderTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                                Padding="12,4">
                            <TextBlock Text="{Binding Key}" 
                                       
                                       FontSize="14"/>
                        </Border>
                    </DataTemplate>
                </GroupStyle.HeaderTemplate>
            </GroupStyle>
        </ListView.GroupStyle>

        <ListView.ItemTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding MonthName}" 
                           Margin="24,0"
                           FontSize="14"
                           Padding="0"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</UserControl>
